name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # Dependencies for this module
  - pyyaml>=5.3.1
  - seamm
  - seamm-util

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    # Testing
    - yapf

    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
