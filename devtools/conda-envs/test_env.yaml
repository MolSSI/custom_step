name: test

channels:
  - default

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - Pmw
    - pyyaml>=5.1
    - seamm>=0.8
    - seamm-util>=0.8
    - seamm-widgets>=0.8
      # Testing
    - yapf

