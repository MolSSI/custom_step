name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - pyyaml>=5.3.1
    - seamm>=2020.12.4
    - seamm-util>=2020.12.2

    # Testing
    - yapf

